// 멘토링 시스템(멘토는 모든 테스트에서 멘티보다 등수가 앞서야 함)

const solution = (test) => {
    let answer=0;
    m=test.length;
    n=test[0].length;

    return answer;
}

let arr=[[3, 4, 1, 2], [4, 3, 2, 1], [3, 1, 4, 2]];
console.log(solution(arr));